---
title: 'useToggle'
date: '2022-04-21'
isPublished: true
---

# useToggle

A simple toggle state.

## Demo

<Youtube id="3PTbTDX6mIU" title="Tests"/>


## The Hook

```tsx
import { useState } from "react"

const useToggle = (initialValue) => {
  const [value, setValue] = useState(initialValue)
  
  const toggleValue = (value) => {
    setValue(val => typeof value === "boolean" ? value : !val)
  }

  return [value, toggleValue]
}

export default useToggle
```

## Usages

[my website](https://test.com)
